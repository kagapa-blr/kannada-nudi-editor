# nudi-python
kannada nudi python version 

## freeze pip into requirements file 
1. pip freeze > requirements.txt
2. install libraries :  pip install -r requirements.txt

### create virtual env
pip install virtualenv

